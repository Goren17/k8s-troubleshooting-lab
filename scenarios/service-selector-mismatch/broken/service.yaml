apiVersion: v1
kind: Service
metadata:
  name: selector-demo
  labels:
    troubleshooting-lab/scenario: service-selector-mismatch
spec:
  selector:
    app: payment-api
  ports:
    - name: http
      port: 80
      targetPort: 80

