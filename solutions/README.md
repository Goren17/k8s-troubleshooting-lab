# Solutions

The solution for each scenario lives next to the broken manifests:

```text
scenarios/<scenario>/README.md
scenarios/<scenario>/fixed/
```

This directory exists as a clear landing point for reviewers, while keeping each fix close to the failure it explains.

